apiVersion: v1
kind: Service
metadata:
  name: springboot-openshift-service
spec:
  type: NodePort
  selector:
    app: cseoc-app
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30007